{
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase": "^9.20.0",
    "firebase-admin": "^11.7.0",
    "mqtt": "^4.3.7",
    "ws": "^8.13.0"
  },
  "engines": {
    "node": "20.0.0"
  }
}
